[server]
headless = true
port = 8501

[global]
developmentMode = false
```

**Step 6** — Click green **"Commit changes"** ✅

---

Now also fix `requirements.txt` — click it in your repo → pencil icon ✏️ → replace everything with:
```
streamlit
pandas
numpy
matplotlib
scikit-learn
pymupdf
spacy==3.7.2
https://github.com/explosion/spacy-models/releases/download/en_core_web_sm-3.7.1/en_core_web_sm-3.7.1-py3-none-any.whl
